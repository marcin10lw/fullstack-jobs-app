{
  "name": "jobify-app",
  "version": "1.0.0",
  "scripts": {
    "start": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm start",
    "start:backend": "cd backend && npm install && npm start",
    "start:frontend": "cd frontend && npm install && npm run build"
  }
}